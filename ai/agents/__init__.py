"""
agents/ — Autonomous agent definitions.

Each agent is responsible for a specific high-level task (e.g. receipt
extraction, spending analysis, anomaly detection).  Agents use tools and
memory to complete multi-step workflows.

Add agent modules here as the system grows:
    - receipt_agent.py  — orchestrates OCR → parse → store pipeline
    - analysis_agent.py — answers spending questions from DB data
    - report_agent.py   — generates periodic spending summaries
"""
